import React, { FC } from 'react';
import styles from './Control.module.scss';

interface ControlProps {
    step: number;
}

const Control: FC<ControlProps> =  ({ step }) => {
    return (
        <>
        </>
    );
}

export default Control;